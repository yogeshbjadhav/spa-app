import { CountFilterPipe } from './count-filter.pipe';

describe('CountFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CountFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
