import { GenderfilterPipe } from './genderfilter.pipe';

describe('GenderfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
